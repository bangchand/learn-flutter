import 'package:flutter/material.dart';

class Vocabnote extends StatefulWidget {
  const Vocabnote({super.key});

  @override
  State<Vocabnote> createState() => _VocabnoteState();
}

class _VocabnoteState extends State<Vocabnote> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('VocabNote')),
      body: Column(children: [
        
      ],),
    );
  }
}
